import { CanvasDirective } from './canvas.directive';

describe('CanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasDirective(null);
    expect(directive).toBeTruthy();
  });
});
